export enum EnumFileTypes {
  MD = ".md",
  JS = ".js",
  TS = ".ts",
  JSON = ".json",
}
export enum PromiseStatus {
  PENDING = "pending",
  FULFILLED = "fulfilled",
  REJECTED = "rejected",
}

export enum EnumFileImageExt {
  PNG = ".png",
  JPG = ".jpg",
  JPEG = ".jpeg",
  SVG = ".svg",
  GIF = ".gif",
  WEBP = ".webp",
}

